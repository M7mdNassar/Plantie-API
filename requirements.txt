# --- Web framework ------------------------------------------------------
fastapi==0.115.0
uvicorn[standard]==0.30.0
gunicorn==23.0.0
sse-starlette==2.1.0
python-dotenv==1.0.0
pydantic==2.9.0
pydantic-settings==2.4.0

# --- Auth -----------------------------------------------------------------
python-jose[cryptography]==3.3.0
httpx==0.27.0

# --- Database ---------------------------------------------------------
supabase==2.6.0

# --- LLM providers ------------------------------------------------------
mistralai==1.5.2
google-generativeai==0.8.0

# --- Embeddings (local, CPU, onnxruntime — no torch needed) ------------
fastembed==0.8.0

# --- RAG ingestion (used by scripts/index_documents.py, not the API hot path) --
langchain-text-splitters==0.3.0
PyMuPDF==1.24.0

# --- Small utilities -----------------------------------------------------
cachetools==7.1.4